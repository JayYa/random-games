# 随机转盘 (Random Wheel)

一个帮人决定"中午吃哪家"的转盘网站。候选饭店来自一份提交在仓库里的 CSV 文件，网页把它画成转盘并随机转出一家。

## Language

### 数据

**名单 (Roster)**：
CSV 文件里的全部饭店条目，包含已停用的。它是这个项目唯一的数据来源。
_Avoid_: 列表、数据源、restaurants（指代整个集合时）

**饭店 (Restaurant)**：
名单中的一条记录，由店名和启用状态构成。
_Avoid_: 餐厅、店铺、选项、item

**停用 (Disabled)**：
一家饭店被标记为不参与随机。停用的饭店既不上转盘，也不在界面上出现，但仍保留在名单中——用于长期停业的店，比删除更可逆。
_Avoid_: 删除、隐藏、禁用（动词）、inactive

### 转盘

**上盘名单 (Lineup)**：
本次真正画在转盘上的那批饭店，最多 12 家。名单中启用的饭店不超过 12 家时，上盘名单就是它们的全部；超过时，上盘名单是从中随机抽取的 12 家。区分"名单"和"上盘名单"是这个项目的核心概念差别。
_Avoid_: 候选、本轮列表、subset

**换一批 (Reshuffle)**：
重新抽取上盘名单。只能由用户显式触发，不会在转动过程中发生。
_Avoid_: 刷新、重新随机、shuffle（单指打乱顺序）

**扇区 (Sector)**：
转盘上对应一家饭店的一块扇形。上盘名单里每家饭店恰好占一个扇区，所有扇区等分圆周。
_Avoid_: 分区、格子、slice、segment

**转一次 (Spin)**：
一次完整的随机过程：选出中选饭店、播放旋转动画、展示结果。
_Avoid_: 抽取、摇号、roll

**中选饭店 (Winner)**：
一次转动的结果。它在动画开始前就已确定，旋转只是把它呈现出来。
_Avoid_: 结果、赢家、选中项
