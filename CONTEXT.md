# 随机转盘 (Random Wheel)

一个帮人做随机决定的转盘网站。先选一个主题（今天吃什么 / 今天玩什么 / 今天干什么），再把这个主题下的候选画成转盘，随机转出一个。每个主题的候选来自一份提交在仓库里的 CSV 文件。

## Language

### 主题

**主题 (Theme)**：
一次随机决定的题目，例如"今天吃什么"。主题是使用这个站点的第一步：先选主题，才有转盘。每个主题自带一份名单和一套面向使用者的说法，主题之间互不相干。
_Avoid_: 分类、场景、模式、category

### 数据

**名单 (Roster)**：
一个主题下的全部候选，包含已停用的。每个主题恰好有一份名单，名单是这个主题唯一的数据来源；不同主题的名单之间不共享候选。
_Avoid_: 列表、数据源、候选集（指代整个集合时）

**候选 (Candidate)**：
名单中的一条记录，由名字和启用状态构成。它在"今天吃什么"里是一家饭店，在"今天干什么"里是一件事——同一个概念，不随主题改名。
_Avoid_: 饭店、条目、选项、item

**停用 (Disabled)**：
一个候选被标记为不参与随机。停用的候选既不上转盘，也不在界面上出现，但仍保留在名单中——用于长期不再考虑的候选，比删除更可逆。
_Avoid_: 删除、隐藏、禁用（动词）、inactive

### 转盘

**上盘名单 (Lineup)**：
本次真正画在转盘上的那批候选，最多 12 个，顺序是打乱的。名单中启用的候选不超过 12 个时，上盘名单就是它们的全部（只是换了顺序）；超过时，上盘名单是打乱后的前 12 个。区分"名单"和"上盘名单"是这个项目的核心概念差别。
_Avoid_: 候选列表、本轮列表、subset

**换一批 (Reshuffle)**：
重新抽取上盘名单，换的是上盘的候选而不只是它们的座次。只能由用户显式触发，不会在转动过程中发生；启用的候选不超过 12 个时无从可换。
_Avoid_: 刷新、重新随机、重排、shuffle（单指打乱顺序）

**扇区 (Sector)**：
转盘上对应一个候选的一块扇形。上盘名单里每个候选恰好占一个扇区，所有扇区等分圆周。
_Avoid_: 分区、格子、slice、segment

**转一次 (Spin)**：
一次完整的随机过程：选出中选、播放旋转动画、展示结果。
_Avoid_: 抽取、摇号、roll

**中选 (Winner)**：
一次转动的结果，上盘名单里被转出来的那个候选。它在动画开始前就已确定，旋转只是把它呈现出来。
_Avoid_: 结果、赢家、选中项
