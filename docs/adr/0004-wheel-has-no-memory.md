# 转盘没有记忆

不做历史记录，不做"避免连续转出同一家"，也不做界面上临时勾选排除某几家的功能。

一旦把上次的结果排除掉，转盘就不再是转盘，而是一个带记忆的推荐系统：结果不可复现、也难以向使用者解释。同理，临时勾选排除等于把"名单"从 CSV 搬进了界面，与 [ADR-0001](./0001-csv-in-repo-no-backend.md) 里"CSV 是唯一数据来源"的前提打架。今天不想吃就再转一次。

如果某家店出现得太频繁真的开始烦人，那是往 CSV 里加权重列的信号，不是加记忆的信号。权重列目前明确推迟。
